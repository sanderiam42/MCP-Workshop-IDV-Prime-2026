services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: demo
      POSTGRES_USER: demouser
      POSTGRES_PASSWORD: demopass123
    volumes:
      - ./pg-init:/docker-entrypoint-initdb.d:ro
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - training-network

  ollama:
    image: ollama/ollama:latest
    environment:
      OLLAMA_KEEP_ALIVE: "24h"
    volumes:
      - ollama-models:/root/.ollama
    ports:
      - "11434:11434"
    # Let the image run its default entrypoint; we just tell it to 'serve'
    command: ["serve"]
    networks:
      - training-network

  ollama-model-puller:
    image: docker:cli
    depends_on:
      - ollama
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./pull-ollama-model.sh:/pull-ollama-model.sh:ro
    networks:
      - training-network
    command: ["sh", "/pull-ollama-model.sh"]

  client:
    image: node:20-slim
    stdin_open: true
    tty: true
    command: >
      bash -c "
        if [ ! -f /root/.setup-complete ]; then
          apt-get update &&
          apt-get install -y git curl jq vim &&
          corepack enable &&
          corepack prepare npm@10 --activate &&
          npm install -g @modelcontextprotocol/server-filesystem @mzxrai/mcp-webresearch pg-mcp-server &&
          touch /root/.setup-complete
        fi &&
        exec bash
      "
    networks:
      - training-network

  auth-server:
    build:
      context: .
      dockerfile: deploy/auth-server.Dockerfile
    environment:
      PORT: "8081"
      DATA_DIR: /app/data
      AUTH_SERVER_ISSUER: ${AUTH_SERVER_ORIGIN}
      # - VERBOSE=true
      # - LOG_FILE=/data/debug.log
    ports:
      - "8081:8081"
    volumes:
      - ./data/auth:/app/data
    networks:
      - training-network

  resource-server:
    build:
      context: .
      dockerfile: deploy/resource-server.Dockerfile
    environment:
      PORT: "8082"
      DATA_DIR: /app/data
      RESOURCE_SERVER_ISSUER: ${RESOURCE_SERVER_ORIGIN}
      AUTH_SERVER_ISSUER: ${AUTH_SERVER_ORIGIN}
      AUTH_SERVER_JWKS_URL: ${AUTH_SERVER_ORIGIN}/oauth/jwks.json
      # - VERBOSE=true
      # - LOG_FILE=/data/debug.log
    ports:
      - "8082:8082"
    depends_on:
      - auth-server
    volumes:
      - ./data/resource:/app/data
    networks:
      - training-network

  requesting-app:
    build:
      context: .
      dockerfile: deploy/requesting-app.Dockerfile
    environment:
      PORT: "3000"
      DATA_DIR: /app/data
      WEB_DIST_DIR: /app/web/dist
      REQUESTING_APP_BASE_URL: ${REQUESTING_APP_ORIGIN}
      AUTH_SERVER_ISSUER: ${AUTH_SERVER_ORIGIN}
      AUTH_SERVER_INTERNAL_URL: ${AUTH_SERVER_ORIGIN}
      RESOURCE_SERVER_ISSUER: ${RESOURCE_SERVER_ORIGIN}
      RESOURCE_SERVER_INTERNAL_URL: ${RESOURCE_SERVER_ORIGIN}
      # - VERBOSE=true
      # - LOG_FILE=/data/debug.log
    ports:
      - "3000:3000"
    depends_on:
      - auth-server
      - resource-server
    volumes:
      - ./data/requesting-app:/app/data
    networks:
      - training-network

volumes:
  pgdata: {}
  ollama-models: {}

networks:
  training-network:
    driver: bridge
